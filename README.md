# Mini-redis-py
A minimal Redis server clone built from scratch in Python, implementing the RESP wire protocol, in-memory key-value storage, and core commands (GET, SET, DELETE, MGET, MSET) over raw sockets with gevent.

